class jeuxdicode::clusters::jeuxdicode::app {

    # cron::job {
    #     "cron" :
    #         minute      => '*/5',
    #         hour        => '*',
    #         date        => '*',
    #         month       => '*',
    #         weekday     => '*',
    #         user        => 'admin',
    #         command     => "ping www.google.fr > /dev/null 2>>/home/admin/logs/jeuxdicode/cron.log",
    #         environment => [ 'SHELL="/bin/bash"', 'PATH="/sbin:/bin:/usr/sbin:/usr/bin:/usr/local/bin"' ];
    # }

}