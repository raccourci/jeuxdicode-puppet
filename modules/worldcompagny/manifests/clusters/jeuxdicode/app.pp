class worldcompagny::clusters::jeuxdicode::app {

    # cron::job {
    #     "cron_helloworld" :
    #         minute      => '*/5',
    #         hour        => '*',
    #         date        => '*',
    #         month       => '*',
    #         weekday     => '*',
    #         user        => 'admin',
    #         command     => "ping www.google.fr > /dev/null 2>>/home/admin/logs/helloworld/cron.log",
    #         environment => [ 'SHELL="/bin/bash"', 'PATH="/sbin:/bin:/usr/sbin:/usr/bin:/usr/local/bin"' ];
    # }

}